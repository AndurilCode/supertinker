{
  "name": "codex",
  "type": "provider",
  "description": "OpenAI Codex CLI provider — `codex exec` with JSONL streaming, thread-resume session persistence, and --output-schema choice enforcement",
  "files": ["codex.ts"],
  "version": "1.1.0"
}
