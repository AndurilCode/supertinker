{
  "name": "gemini",
  "type": "provider",
  "description": "Google Gemini CLI provider — `gemini -p -o stream-json` with JSONL streaming and text-sentinel choice parsing",
  "files": ["gemini.ts"],
  "version": "1.1.0"
}
